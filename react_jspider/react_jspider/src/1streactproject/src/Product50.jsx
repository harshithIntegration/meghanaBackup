import React from "react";
const Product50=(props)=>{
    return(
<div>
    <h1>{props.product_name}</h1>
    <h1>{props.product_id}</h1>
    <h1>{props.product_price}</h1>
    <h1>{props.product_quantity}</h1>
</div>
    )
}
export default Product50;